import React from "react";
import MoreServiceCard from "../../Common/MoreService/MoreServiceCard";

const DetailsServiceLink = ({services}) => {

  return (
    <div>
      <MoreServiceCard services={services} />
    </div>
  );
};

export default DetailsServiceLink;
